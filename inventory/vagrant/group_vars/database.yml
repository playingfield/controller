---
postgres_enabled: true
postgres_version: 15
# Postgresql users and databases/schemas
database:
  postgres:
    name: postgres
    owner: postgres
    username: postgres
    password: your_database_password
    enabled: true
  semaphore:
    name: semaphore
    owner: semaphore
    username: semaphore
    password: your_database_password
    enabled: true
